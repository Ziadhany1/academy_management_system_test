<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="academy_enrollment_list" model="ir.ui.view">
        <field name="name">academy.enrollment.list</field>
        <field name="model">academy.enrollment</field>
        <field name="arch" type="xml">
            <list>
                <field name="student_id"/>
                <field name="course_id"/>
                <field name="state"/>
                <field name="grade"/>
                <field name="attendance_percentage"/>
                <field name="passed"/>
            </list>
        </field>
    </record>

    <record id="academy_enrollment_form" model="ir.ui.view">
        <field name="name">academy.enrollment.form</field>
        <field name="model">academy.enrollment</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_confirm" type="object" string="Confirm" modifiers="{'invisible': [('state','!=','draft')]}"/>
                    <button name="action_complete" type="object" string="Complete" modifiers="{'invisible': [('state','!=','confirmed')]}"/>
                    <button name="action_cancel" type="object" string="Cancel" modifiers="{'invisible': [('state','in',['completed','cancelled'])]}"/>
                    <field name="state" widget="statusbar"/>
                </header>

                <sheet>
                    <group>
                        <field name="student_id"/>
                        <field name="course_id"/>
                        <field name="enrollment_date"/>
                        <field name="grade"/>
                        <field name="attendance_percentage"/>
                        <field name="passed" readonly="1"/>
                        <field name="notes"/>
                    </group>
                </sheet>

                <chatter/>
            </form>
        </field>
    </record>

</odoo>
